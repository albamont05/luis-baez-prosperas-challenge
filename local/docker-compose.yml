services:
  # Base de Datos (Emulando Amazon RDS)
  postgres:
    container_name: postgres_db
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    env_file:
      - ../.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U prosperas_user -d reports_db" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # Emulador de AWS (SQS, S3, etc.)
  localstack:
    container_name: localstack_main
    image: localstack/localstack:4.14
    ports:
      - "4566:4566"
    environment:
      - SERVICES=sqs,s3
      - AWS_DEFAULT_REGION=us-east-1
      - GATEWAY_LISTEN_PORT=4566
      - PERSISTENCE=1
      - PERSIST_ALL=1
      - SNAPSHOT_SAVE_STRATEGY=ON_REQUEST
      - DOCKER_FLAGS=-v /var/run/docker.sock:/var/run/docker.sock
      - SAVE_STATE=1
      - DEBUG=1
    stop_grace_period: 45s
    volumes:
      - "./init-aws.sh:/etc/localstack/init/ready.d/init-aws.sh"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "localstack_data:/var/lib/localstack"
      - "localstack_state:/usr/lib/localstack"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:4566/_localstack/health" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # API Backend (FastAPI)
  api:
    container_name: fastapi_reports_api
    build:
      context: ../backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      localstack:
        condition: service_healthy

  # Background Worker
  worker:
    build:
      context: ../backend
      dockerfile: Dockerfile
    command: [ "python", "-m", "app.worker.main" ]
    deploy:
      replicas: 2
    env_file:
      - ../.env
    environment:
      - AWS_ENDPOINT_URL=http://localstack:4566
    restart: on-failure
    depends_on:
      api:
        condition: service_started
      localstack:
        condition: service_healthy

  # Frontend (React)
  frontend:
    container_name: react_frontend
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        # En desarrollo local, el navegador del usuario accede vía localhost:8000
        - VITE_API_BASE_URL=http://localhost:8000
    ports:
      - "80:80"
    restart: always
    depends_on:
      - api

volumes:
  postgres_data:
  localstack_data:
  localstack_state:
